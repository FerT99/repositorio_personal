<%- include('includes/header.ejs') %>

<h1>Aquí veremos los productos disponibles.</h1>

<% if (productos.length > 0) { %>
    <% for (let producto of productos) { %>
        <div class="card" style="width: 18rem;"></div>
            <img src="../../lab6/media/descarga.jpg" class="card-img-top" alt="img sueter">
            <div class="card-body">
                <p class="card-text"><%= productos.producto %></p>
            </div>
        </div>
    <% } %>
<% } else { %>
    <h3>No tenemos pedidos activos.</h3>
<% } %>
</div>

<a href="/tienda/pedidos">Agregar un nuevo pedido.</a>

</main>